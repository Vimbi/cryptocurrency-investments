import { FC } from 'react';

import { Grid } from '@mui/material';

const RefundSettings: FC = () => {
	return (
		<Grid container spacing={6}>
			<Grid item xs={12}></Grid>
		</Grid>
	);
};

export default RefundSettings;
